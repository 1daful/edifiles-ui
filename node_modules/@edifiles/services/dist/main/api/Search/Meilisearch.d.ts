import { MeiliSearch } from 'meilisearch';
import { Filter, FilterCheck, FilterCustom, FilterRange } from "../../utility/Types";
export declare class Meilisearch {
    client: MeiliSearch;
    test(collName: any): void;
    search(collName: string, query: string, filter: Filter, sort: {
        attributes: string;
        order: string;
    }): Promise<import("meilisearch").SearchResponse<Record<string, any>, {
        filter: any[];
        sort: string[];
    }>>;
    index(collName: string, items: Record<string, any>[]): void;
    updateSettings(index: string, filterablesAttributes: any, sortableAttributes: any): void;
    sort(index: string): void;
    filters(filters: FilterCustom[]): any[][];
    filter(filter: FilterCustom): string[];
    checkFilter(filterCheck: FilterCheck): string[];
    checkFilters(filterChecks: FilterCheck[]): any[][];
    rangeFilter(filterRange: FilterRange): string;
    rangeFilters(filterRanges: FilterRange[]): string[];
}
//# sourceMappingURL=Meilisearch.d.ts.map